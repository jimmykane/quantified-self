export const QueueErrors = {
    NO_TOKEN_FOUND: 'No token found - Fail Fast',
    ALL_TOKENS_FAILED: 'All token processing attempts failed'
};

export const QueueLogs = {
    NO_TOKEN_FOUND: 'No token found for queue item ${id}. Fail fast.'
};

export const MAIL_TTL_DAYS = 90;
