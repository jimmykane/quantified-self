import {EventImporterJSON} from './importer.json';

describe('EventImporterJSON', () => {

  let eventImporter: EventImporterJSON;

  beforeEach(() => {
    eventImporter = new EventImporterJSON();
  });

  it('should get a value of 60', () => {
  });

});
