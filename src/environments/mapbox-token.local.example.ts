// Copy this file to mapbox-token.local.ts and replace the placeholder with a
// public token created for local development. Never commit the copied file.
export const mapboxAccessToken = 'YOUR_PUBLIC_MAPBOX_TOKEN';
